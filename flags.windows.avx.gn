# AVX optimization flags
# Requires CPUs with AVX instruction set support (Sandy Bridge/Bulldozer or newer)
# Note: Actual AVX compiler flags are set via patches (windows-enable-avx.patch)
# which modify build/config/win/BUILD.gn and build/config/compiler/BUILD.gn
