# AVX2 optimization flags
# Requires CPUs with AVX2 instruction set support (Haswell/Excavator or newer)
# Note: Actual AVX2 compiler flags are set via patches (windows-enable-avx2.patch)
# which modify build/config/win/BUILD.gn and build/config/compiler/BUILD.gn
