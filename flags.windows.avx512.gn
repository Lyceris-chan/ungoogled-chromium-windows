# AVX512 optimization flags
# Requires CPUs with AVX512 instruction set support (Skylake-X/Zen 4 or newer)
# Note: Actual AVX512 compiler flags are set via patches (windows-enable-avx512.patch)
# which modify build/config/win/BUILD.gn and build/config/compiler/BUILD.gn
